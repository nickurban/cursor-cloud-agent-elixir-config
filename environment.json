{
  "install": "bash .cursor/install.sh",
  "start": "sudo ntpdate -s pool.ntp.org && sudo service postgresql start && mix ash.setup",
  "terminals": [
    {
      "name": "Phoenix Server",
      "command": "PORT=4000 iex -S mix phx.server"
    }
  ],
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  }
}